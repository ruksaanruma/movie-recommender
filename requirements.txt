# Core
numpy>=1.26
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3        # cosine similarity, TruncatedSVD, metrics

# Visualization
matplotlib>=3.7
seaborn>=0.13

# Optional: the scikit-surprise library provides a clean SVD implementation.
# If it fails to install on your platform, this project also includes a
# from-scratch matrix-factorization fallback (no extra dependency needed).
# scikit-surprise>=1.1
